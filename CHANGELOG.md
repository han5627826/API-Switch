# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.2.3] - 2026-09-16

### 新增
- **「卸载并清除数据」**：网络设置弹窗左下角新增红色入口。一键完成：还原 Codex 原始配置（用首次修改前的备份覆盖回 `config.toml` / `auth.json`）→ 删除工具数据目录（供应商库与其中 DPAPI 加密的 API Key，不可恢复）→ 删除模型规格缓存。已导入到 Qoder / ZCode / TRAE 的模型属于各软件自己的配置，不受影响。
- README 新增三张原始界面截图（目标管理首页 / 添加供应商 / Codex 页）。

## [1.2.2] - 2026-09-16

### 新增
- **右上角「检查更新」按钮**：位于「网络设置」右侧同一行，可随时手动检查是否为最新版本——点击后显示「检查中…」，完成后提示结果（最新版本 / 发现新版本）；发现新版本时旁边会出现「发现新版本 vX.Y.Z」一键更新按钮。

## [1.2.1] - 2026-09-16

### 修复
- **更新检查代理回退**：一键更新检查 / 下载此前只走单一代理路径——代理出口 IP 触发 GitHub 匿名限流（HTTP 403，共享代理极常见）或代理连接失败时，检查静默失败、用户收不到新版本提示。现在任何代理失败后都会自动回退直连重试一次，覆盖三种场景：应用内代理失败、系统代理失败、未配置代理时系统代理失败。

## [1.2.0] - 2026-09-16

### 变更
- **界面重设计（A · 极光玻璃方向）**：全站切换为浅色玻璃拟态视觉——极光渐变光斑背景、漂浮光珠、半透明卡片、细边框与柔和阴影、渐变主按钮与页签光条；移除旧版蓝色扁平风格。
- **目标管理首页化**：「目标管理」移至第一页签并标记「首页」，成为默认打开视图；新增总控台（供应商数 / 模型总数 / 已检测软件统计）。
- **供应商集中管理**：添加 / 编辑 / 删除供应商统一收进「目标管理 · 供应商库」（卡片网格 + 悬浮操作）；Codex 页改为只读展示（保留切换配置与重启），移除页内「添加供应商」入口。
- **各软件页自动识别**：Codex / Qoder / Qoder CN / ZCode / TRAE 页顶部新增识别横幅（供应商数量、目标检测状态），下拉直接读取供应商库，不再提供录入入口。
- 细节调整（同日）：Codex 页横幅「前往供应商库 / 重启 Codex」改为渐变主按钮；各页列表行内「启用 / 删除」按钮放大；正文与标题字号有选择地适度放大（徽标、说明性小字保持紧凑）；「配置写入」等侧卡标签强制单行（路径超长省略号 + 悬停提示）；供应商库不再显示「添加供应商」虚线卡片（统一走「＋ 新增」按钮）。
- 极光氛围补全：新增零依赖 Canvas 伪 3D 背景层（粒子场 + 二十面体/八面体线框 + 鼠标视差，离线可用，尊重 prefers-reduced-motion）；总控台在默认窗口（1120×800）下恢复单行布局并收紧字号。

### 兼容
- 后端接口零变更（仅前端三件套重做）；`replacements.json` 全部 HTML 锚点保留，repack / feature_pack 构建自检通过。

## [1.1.0] - 2026-09-15

### 新增
- **ZCode 页签**：一键导入供应商模型到 `~/.zcode/v2/config.json`（与 ZCode 手动添加的自定义供应商格式一致），支持列表中「启用」一键切换（互斥启用）与删除。
- **TRAE Work CN 模型管理**：读取 TRAE `state.vscdb` 中的自定义模型列表，支持启用/停用（写回）与删除；辅助导入沿用剪贴板方案。
- **「目标管理」页签**：所有受支持软件的检测状态、写入位置与导入步骤一览；未检测到的软件给出置灰提示，解决首装用户不知道去哪导入的问题。
- 添加供应商保存后的提示会引导到各目标页签导入；所有导入页共用统一的「选供应商 → 勾模型 → 一键导入」布局，并自动附带模型上下文/输出/视觉/推理规格。

### 修复
- **Qoder CN 后端路由丢失**（v1.0.0 回归）：打包时替换 `make_server` 导致 `/api/qodercn/*` 接口缺失、Qoder CN 页签不可用；本版恢复并纳入统一注入模板。
- Qoder 页签改版：旧版写 `state.vscdb` 的 `aicoding.customModels`（受云端 BYOK 白名单限制，新版 Qoder 已弃用）；改为写入 `~/.qoder/settings.json` 的 `providers`（Qoder 当前实际使用的自定义模型格式，即你手动添加模型时它写的那份），一键导入即刻生效。
- 单实例保护：禁止两个实例同时监听 8765 端口导致请求随机分发、配置错乱。

### 变更
- 后端注入改为单一可测模板 `tools/feature_backend.py`（Qoder / Qoder CN / ZCode / TRAE / 更新路由一体），28 项单元测试覆盖全部新接口。

## [1.0.0] - 2026-09-15

### 新增
- 首个公开发布版本：Codex / Qoder / Trae / Qoder CN 四个页签的一键配置与切换能力。
- 内置「一键更新」：启动时静默比对 GitHub Releases 最新版本，发现新版时右上角提示；确认后自动下载、SHA-256 校验、覆盖安装并自动重启。
- 全新安装修复：首次运行（无任何配置文件）时供应商库初始化不完整导致界面加载失败的问题。

### 变更
- 数据目录独立为 `%APPDATA%\APISwitch`，与任何早期版本互不干扰。

